# pnpm monorepo settings
link-workspace-packages=true
prefer-workspace-packages=true
# Keep node_modules layout predictable for native modules (better-sqlite3, ssh2)
shamefully-hoist=false
# Store on disk even if a build step modifies package dir (electron-builder)
node-linker=isolated
# Electron postinstall binary download
electron_mirror=
