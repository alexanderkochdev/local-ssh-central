{
  "name": "sample-plugin",
  "version": "1.0.0",
  "description": "Beispiel-Plugin fuer SSH Central - zeigt Hooks, Events, UI-Tab, IPC, Dialoge, Secrets und Storage",
  "main": "index.js",
  "sshCentral": {
    "tabs": [{ "id": "dashboard", "label": "Dashboard" }],
    "ui": { "entry": "ui/index.html" },
    "permissions": { "hosts": true }
  }
}
